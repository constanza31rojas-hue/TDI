# TDI

Sitio público de ejemplo incluido en este repositorio. El asistente creó una página estática en `index.html` que puedes publicar con GitHub Pages (Settings → Pages) o desplegar con Vercel/Netlify.

Si GitHub Pages está activado en la rama `main` con la carpeta raíz, la URL será:

`https://constanza31rojas-hue.github.io/TDI`
